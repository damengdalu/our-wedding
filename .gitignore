prompt.md
ideas.md
wedding.zip
.thumbnail
README.md

# Plaintext content — NEVER commit this; only the encrypted js/content.js ships
content/content.template.js

# Real gallery photos — NEVER commit these; only referenced by path inside the
# encrypted js/content.js payload
images/gallery/
images/gallery-china/

# Cloudflare Worker local cache — contains account email/ID, never commit
tools/rsvp-worker/.wrangler/
